<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blog.mapper.CommentMapper">

  <resultMap id="CommentVOMap" type="com.example.blog.dto.CommentVO">
    <id column="id" property="id"/>
    <result column="content" property="content"/>
    <result column="author_name" property="authorName"/>
    <result column="created_at" property="createdAt"/>
  </resultMap>

  <select id="selectById" resultMap="CommentVOMap">
    SELECT c.id, c.content, c.created_at, u.username AS author_name
    FROM comment c
    LEFT JOIN user u ON c.user_id = u.id
    WHERE c.id = #{id}
  </select>

  <select id="selectByBlogId" resultMap="CommentVOMap">
    SELECT c.id, c.content, c.created_at, u.username AS author_name
    FROM comment c
    LEFT JOIN user u ON c.user_id = u.id
    WHERE c.blog_id = #{blogId}
    ORDER BY c.created_at ASC
  </select>
</mapper>
