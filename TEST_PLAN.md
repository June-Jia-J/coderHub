# CoderHub 闭环测试验证方案

## 测试范围

验证之前修复的所有5个核心问题，确保功能完整可用。

---

## 📋 测试用例清单

### ✅ 测试用例 1: 本地开发联调测试
**前置条件：** 后端启动在 8207 端口，前端执行 `npm run dev`

| 步骤 | 预期结果 | 验证点 |
|------|----------|--------|
| 1. 启动后端: `mvn spring-boot:run` | 后端监听 8207 端口 | `http://localhost:8207/health` 返回 200 |
| 2. 启动前端: `npm run dev` | 前端启动在 5173 端口 | Vite 代理配置正确指向 8207 |
| 3. 访问前端首页 | 页面正常加载，无网络错误 | 浏览器 Network 面板无 502/连接拒绝错误 |

---

### ✅ 测试用例 2: 博客列表接口测试
**前置条件：** 后端服务正常运行

| 步骤 | 预期结果 | 验证点 |
|------|----------|--------|
| 1. 调用 GET `/api/blogs` | 返回 200，包含博客列表数据 | `code === 200`，`data.list` 为数组 |
| 2. 调用 GET `/api/blogs?author=admin` | 返回该作者的博客列表 | 过滤正确 |
| 3. 前端首页"最新博客" | 列表正常渲染，无控制台报错 | 前端调用路径正确 `/api/blogs` (带s) |

---

### ✅ 测试用例 3: 用户登录与鉴权测试
**前置条件：** 测试账号 `admin/admin123` 已存在

| 步骤 | 预期结果 | 验证点 |
|------|----------|--------|
| 1. 提交登录表单 | 登录成功，跳转首页 | localStorage 中 `blog_token` 和 `blog_username` 正确 |
| 2. 检查 Header 显示 | 右上角显示正确用户名 | 不是显示 token 字符串 |
| 3. 发布新博客 | 创建成功，返回 200 | Authorization header 正确携带 Bearer Token |
| 4. 发表评论 | 评论成功 | JWT 被后端正确识别，无 401 错误 |

---

### ✅ 测试用例 4: 博客详情权限测试
**前置条件：** 用作者账号登录，创建一篇测试文章

| 步骤 | 预期结果 | 验证点 |
|------|----------|--------|
| 1. 作者打开自己的文章 | 显示"编辑"和"删除"按钮 | `isAuthor === true` 逻辑正确 |
| 2. 登出后打开同一文章 | 不显示编辑/删除按钮 | |
| 3. 用其他账号登录打开 | 不显示编辑/删除按钮 | 权限判断正确 |

---

### ✅ 测试用例 5: 编辑文章内容测试
**前置条件：** 存在一篇标题为"测试标题"，内容为"测试正文内容"的文章

| 步骤 | 预期结果 | 验证点 |
|------|----------|--------|
| 1. 进入文章编辑页 | 标题框显示"测试标题" | 标题内容不错位 |
| 2. 检查正文框 | 显示"测试正文内容" | 正文内容不错位 |
| 3. 修改后保存 | 修改后的内容正确保存 | |

---

## 🔧 自动化测试脚本

### 后端 API 测试
执行：`node test-api.js`

### 前端手动测试清单
见 `test-frontend-manual.md`
